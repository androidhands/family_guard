import 'package:flutter/material.dart';

class CustomJsonImage extends StatelessWidget {
  const CustomJsonImage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
